spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tsinjo_preprod
    username: preprod_user
    password: preprod_password
    driver-class-name: org.postgresql.Driver

  flyway:
    locations: classpath:db/migration,classpath:db/preprod
    baseline-on-migrate: true
